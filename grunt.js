
// This file bootstraps the `grunt.coffee` file, that way the `grunt` command works.

require('coffee-script'); // Adds coffee-script support to 'require'

module.exports = require('./grunt.coffee');
